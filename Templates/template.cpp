#include <bits/stdc++.h>
using namespace std;

// --- Abkürzungen & Makros ---
typedef long long ll;
typedef vector<int> vi;
typedef pair<int, int> ii;
#define sz(a) int((a).size())
#define all(c) (c).begin(), (c).end()
#define pb push_back

void solve() {
    // Lösung
}

int main() {
    // Fast I/O
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // Mehrere Testcases
    int t = 1;
    cin >> t; // Bei nur einem Testcase auskommentiere
    while (t--) {
        solve();
    }

    return 0;
}
